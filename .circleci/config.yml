version : 2.1

orbs: 
  heroku: circleci/heroku@0.0.10

commands:
  install-modules:
    steps:
      - checkout
      - run: npm ci
executors:
  node:
    docker:
      - image: circleci/node:12

workflows:
  heroku_deploy:
    jobs:
      - install-modules
      - heroku/deploy-via-git